import { createWidget } from 'discourse/widgets/widget';
import { showSelector } from "discourse/lib/emoji/toolbar";
import { ajax } from 'discourse/lib/ajax';

export default createWidget('rating-composer', {

});
